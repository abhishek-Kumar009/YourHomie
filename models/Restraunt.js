class Restraunt {
    constructor(id, title, category, imageUrl, ratings) {
        this.id = id,
            this.title = title,
            this.category = category,
            this.imageUrl = imageUrl,
            this.ratings = ratings;

    }
}

export default Restraunt;